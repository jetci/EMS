JWT_SECRET=your_jwt_secret_here_change_in_production
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
DATABASE_URL=sqlite://./db/wecare.db
PORT=3001
NODE_ENV=development
# Optional
PGHOST=localhost
PGUSER=wecare
PGPASSWORD=change_me
PGDATABASE=wecare_db
PGPORT=5432

# Notes:
# - Do NOT commit real secrets. Use your platform secret store in production.
# - In production set NODE_ENV=production and a strict ALLOWED_ORIGINS list.
# EMS WeCare Backend - Environment Variables
# Copy this file to .env and fill in your values

# ============================================
# REQUIRED IN ALL ENVIRONMENTS
# ============================================

# JWT Secret for token signing (REQUIRED)
# Generate a strong random string (min 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ============================================
# ENVIRONMENT CONFIGURATION
# ============================================

# Environment: development | staging | test | production
NODE_ENV=development

# Server Port
PORT=3001

# ============================================
# CORS CONFIGURATION
# ============================================

# Allowed Origins (REQUIRED in production)
# Format: Comma-separated list of allowed origins
# 
# Development (optional - defaults to localhost):
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
#
# Staging (optional - defaults to localhost):
# ALLOWED_ORIGINS=https://staging.wecare.example.com
#
# Production (REQUIRED):
# ALLOWED_ORIGINS=https://wecare.example.com,https://app.wecare.com
#
# ⚠️  Security Notes:
# - Do NOT use wildcards (*)
# - Do NOT use http:// in production (use https://)
# - Only include trusted domains
# - No spaces around commas
#
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ============================================
# DATABASE CONFIGURATION
# ============================================

# SQLite Database Path (relative to project root)
DATABASE_PATH=./db/wecare.db

# ============================================
# SECURITY CONFIGURATION
# ============================================

# Session Configuration
SESSION_SECRET=your-session-secret-change-this
SESSION_MAX_AGE=3600000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CSRF Protection
CSRF_COOKIE_NAME=_csrf
CSRF_COOKIE_SECURE=false

# ============================================
# LOGGING CONFIGURATION
# ============================================

# Log Level: error | warn | info | debug
LOG_LEVEL=info

# Log to File
LOG_TO_FILE=false
LOG_FILE_PATH=./logs/app.log

# ============================================
# FILE UPLOAD CONFIGURATION
# ============================================

# Upload Directory
UPLOAD_DIR=./uploads

# Max File Size (in bytes)
MAX_FILE_SIZE=5242880

# Allowed File Types
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp,application/pdf

# ============================================
# EMAIL CONFIGURATION (Optional)
# ============================================

# SMTP Configuration
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Email From Address
# EMAIL_FROM=noreply@wecare.example.com

# ============================================
# EXTERNAL SERVICES (Optional)
# ============================================

# Google Maps API Key
# GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# SMS Service (Twilio)
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_PHONE_NUMBER=+1234567890

# ============================================
# MONITORING & ANALYTICS (Optional)
# ============================================

# Sentry DSN for Error Tracking
# SENTRY_DSN=https://your-sentry-dsn

# Google Analytics
# GA_TRACKING_ID=UA-XXXXXXXXX-X

# ============================================
# PRODUCTION-SPECIFIC SETTINGS
# ============================================

# When NODE_ENV=production, also set:
# - ALLOWED_ORIGINS (REQUIRED)
# - JWT_SECRET (use strong random value)
# - SESSION_SECRET (use strong random value)
# - CSRF_COOKIE_SECURE=true
# - LOG_LEVEL=warn or error

# ============================================
# EXAMPLE PRODUCTION .env
# ============================================
#
# NODE_ENV=production
# PORT=3001
# JWT_SECRET=<generated-with-openssl-rand-base64-32>
# SESSION_SECRET=<generated-with-openssl-rand-base64-32>
# ALLOWED_ORIGINS=https://wecare.example.com,https://app.wecare.com
# CSRF_COOKIE_SECURE=true
# LOG_LEVEL=warn
# DATABASE_PATH=./db/wecare.db
#
