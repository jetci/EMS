# แผนการปรับปรุงระบบ EMS WeCare (Improvement Plan)
**ฉบับร่างโดย:** Assistant Team
**วันที่:** 30 มกราคม 2026
**อ้างอิง:** Expert QA Assessment Report

เอกสารฉบับนี้รวบรวมแผนการปรับปรุงระบบเพื่อเตรียมพร้อมสู่ Production โดยแบ่งตามความเร่งด่วนและประเภทของงาน

---

## 📅 เฟส 1: แก้ไขข้อผิดพลาดเร่งด่วนและคุณภาพโค้ด (P0 - Immediate)
**เป้าหมาย:** สภาพแวดล้อมเสถียร ระบบ Build ผ่านทั้งหมด ไม่มี Error พื้นฐาน

1.  **[QA-001] Fix TypeScript Build Errors**
    *   **ปัญหา:** พบ Error `TS2307` ใน `ChangePasswordModal.tsx` เนื่องจากการตั้งค่า Path Alias (`@/src/...`) ไม่สมบูรณ์ใน `tsconfig.json` หรือ Vite Config
    *   **การแก้ไข:** เปลี่ยนการ Import เป็น Relative Path ปกติ (`../../components/ui/...`) หรือแก้ไขการตั้งค่า Alias ให้ถูกต้องทั้งระบบ
    *   **ผู้รับผิดชอบ:** Dev Team

2.  **[QA-002] Improve Error Handling in API Client**
    *   **ปัญหา:** `api.ts` ทำการ `window.location.reload()` ทันทีเมื่อเจอ 401/403 ซึ่งรบกวน UX
    *   **การแก้ไข:** เปลี่ยน Logic ให้ Redirect ไปหน้า Login อย่างนุ่มนวล หรือแสดง Modal แจ้งเตือน session expired แทนการ Reload
    *   **ผู้รับผิดชอบ:** Dev Team

---

## 🎨 เฟส 2: ยกระดับประสบการณ์ผู้ใช้ (P1 - UX Improvements)
**เป้าหมาย:** ระบบมีความเป็นมืออาชีพ ใช้งานง่าย ลดความสับสน

3.  **[UX-001] Replace Native Alerts**
    *   **ปัญหา:** การใช้ `window.alert` และ `window.confirm` ดูไม่ทันสมัยและไม่สอดคล้องกับ Design System
    *   **การแก้ไข:**
        *   นำ `ConfirmationModal` ที่มีอยู่แล้วมาใช้แทน `window.confirm` ในหน้า:
            *   `AdminUserManagementPage.tsx` (ยืนยันเปิด/ปิด User)
            *   `AdminSystemSettingsPage.tsx` (ถ้ามี)
        *   สร้าง/ใช้งาน **Toast Notification** สำหรับแจ้ง Success/Error message แทน `alert()`

4.  **[UX-002] Responsive Tables**
    *   **ปัญหา:** ตารางข้อมูลแสดงผลยากบนจอมือถือ
    *   **การแก้ไข:** ปรับ CSS ให้ตารางมี Scroll แนวนอนที่ลื่นไหล หรือเปลี่ยนเป็น Card View เมื่อหน้าจอเล็กกว่า `md` viewport

---

## 🗄️ เฟส 3: ปรับโครงสร้างข้อมูล (P2 - Backend & Data)
**เป้าหมาย:** รองรับการทำ Analytics และ Reporting ในอนาคต

5.  **[DB-001] Normalize Patient Diseases**
    *   **ปัญหา:** ข้อมูลโรคประจำตัว (`chronic_diseases`) เก็บเป็น JSON String ทำให้ Query ยาก
    *   **การแก้ไข:**
        *   สร้างตารางใหม่ `patient_diseases` (id, patient_id, disease_name, diagnosed_date)
        *   เขียน Script Migrate ข้อมูลจาก JSON ลงตารางใหม่
        *   ปรับ `patients.ts` และ `users.ts` ให้ Join ตารางใหม่แทนการอ่าน JSON

6.  **[SEC-001] Rate Limiting Tuning**
    *   **ปัญหา:** ค่า Default ของ Rate Limit อาจต่ำเกินไปสำหรับการใช้งานจริงในบาง Endpoint
    *   **การแก้ไข:** ตรวจสอบและปรับจูนค่าใน `rateLimiter.ts` ให้เหมาะสมกับ Load ที่คาดการณ์ (เช่น Driver Location Updates อาจต้องสูงกว่า Login)

---

## 🚀 เฟส 4: ความพร้อมสู่ Production (P3 - Deployment)
**เป้าหมาย:** ระบบทนทาน รองรับ Load ได้จริง

7.  **[OPS-001] Database Backup Strategy**
    *   **แผน:** ตั้งค่า Cron Job เพื่อ Backup `wecare.db` ทุกวัน ส่งไปยัง Cloud Storage ภายนอก (เนื่องจาก SQLite เป็นไฟล์เดียว จึง Backup ง่ายแต่เสี่ยงถ้า Server หาย)

8.  **[OPS-002] Monitoring**
    *   **แผน:** ติดตั้ง Dashboard ง่ายๆ เพื่อดู Health ของ Server (CPU, RAM, Disk, Error Logs)

---

**Next Action:** เริ่มดำเนินการ **[QA-001]** และ **[UX-001]** ทันทีหรือไม่?
