# 🧪 คู่มือทดสอบ Profile Image Upload

**วันที่:** 29 มกราคม 2569  
**เวลา:** 18:12 น.

---

## ✅ สถานะระบบ

### Backend
- ✅ รันอยู่ที่: http://localhost:3001
- ✅ API endpoint: `/api/auth/upload-profile-image`
- ✅ Upload directory: `wecare-backend/uploads/profiles/`
- ✅ Multer configured

### Frontend
- ✅ รันอยู่ที่: http://localhost:5175
- ✅ ProfilePage updated
- ✅ Upload UI ready

---

## 📝 ขั้นตอนการทดสอบ

### Test 1: อัพโหลดรูปภาพสำเร็จ ✅

**ขั้นตอน:**
```
1. เปิด http://localhost:5175
2. Login (ใช้ Quick Login หรือ email/password)
   - Email: community1@wecare.dev
   - Password: password
3. คลิกที่เมนู "โปรไฟล์" หรือไปที่ /profile
4. Hover เมาส์ไปที่รูปโปรไฟล์ (วงกลมสีขาว)
5. ควรเห็นไอคอนกล้อง 📷 ปรากฏขึ้น
6. คลิกที่รูปโปรไฟล์
7. เลือกไฟล์รูปภาพ (JPG, PNG, หรือ WEBP)
8. รูปภาพควรแสดง preview ทันที
9. ปุ่ม "บันทึกรูปภาพ" และ "ยกเลิก" ควรปรากฏ
10. คลิก "บันทึกรูปภาพ"
11. ควรเห็น loading spinner
12. ควรเห็นข้อความ "✅ อัพโหลดรูปภาพเรียบร้อยแล้ว"
13. รูปภาพใหม่ควรแสดงในโปรไฟล์
```

**ผลลัพธ์ที่คาดหวัง:**
- ✅ รูปภาพแสดงใน preview
- ✅ อัพโหลดสำเร็จ
- ✅ รูปภาพใหม่แสดงในโปรไฟล์
- ✅ ไฟล์ถูกบันทึกใน `wecare-backend/uploads/profiles/`
- ✅ Database มี `profile_image_url` อัพเดท

---

### Test 2: Validation - File Type ❌

**ขั้นตอน:**
```
1. ทำตามขั้นตอน 1-6 จาก Test 1
2. เลือกไฟล์ที่ไม่ใช่รูปภาพ (เช่น .pdf, .txt, .docx)
```

**ผลลัพธ์ที่คาดหวัง:**
- ❌ ไม่สามารถเลือกไฟล์ได้ (file input filter)
- หรือ
- ❌ แสดงข้อความ error: "กรุณาเลือกไฟล์รูปภาพ (JPG, PNG, WEBP)"

---

### Test 3: Validation - File Size ❌

**ขั้นตอน:**
```
1. ทำตามขั้นตอน 1-6 จาก Test 1
2. เลือกไฟล์รูปภาพที่มีขนาด > 5MB
```

**ผลลัพธ์ที่คาดหวัง:**
- ❌ แสดงข้อความ error: "ขนาดไฟล์ต้องไม่เกิน 5MB"
- ❌ ไม่มี preview แสดง
- ❌ ไม่มีปุ่มบันทึกปรากฏ

---

### Test 4: ยกเลิกการอัพโหลด 🔄

**ขั้นตอน:**
```
1. ทำตามขั้นตอน 1-8 จาก Test 1
2. คลิกปุ่ม "ยกเลิก"
```

**ผลลัพธ์ที่คาดหวัง:**
- ✅ รูปภาพกลับไปเป็นรูปเดิม (หรือ icon ถ้าไม่มีรูป)
- ✅ ปุ่ม "บันทึกรูปภาพ" และ "ยกเลิก" หายไป
- ✅ ไม่มีการอัพโหลดไปยัง server

---

### Test 5: แทนที่รูปภาพเดิม 🔄

**ขั้นตอน:**
```
1. อัพโหลดรูปภาพ A (ตาม Test 1)
2. อัพโหลดรูปภาพ B (ทำซ้ำ Test 1 อีกครั้ง)
```

**ผลลัพธ์ที่คาดหวัง:**
- ✅ รูปภาพ B แสดงในโปรไฟล์
- ✅ รูปภาพ A ถูกลบออกจาก `uploads/profiles/`
- ✅ Database มี `profile_image_url` ใหม่

---

### Test 6: Hover Effect 🎨

**ขั้นตอน:**
```
1. ไปที่หน้า Profile
2. Hover เมาส์ไปที่รูปโปรไฟล์
3. เอาเมาส์ออก
4. Hover อีกครั้ง
```

**ผลลัพธ์ที่คาดหวัง:**
- ✅ Hover: ไอคอนกล้อง 📷 ปรากฏพร้อม overlay สีดำโปร่งแสง
- ✅ ไม่ Hover: ไอคอนหายไป แสดงรูปปกติ
- ✅ Transition ราบรื่น

---

### Test 7: Loading State ⏳

**ขั้นตอน:**
```
1. ทำตามขั้นตอน 1-10 จาก Test 1
2. สังเกต UI ขณะอัพโหลด
```

**ผลลัพธ์ที่คาดหวัง:**
- ✅ ปุ่ม "บันทึกรูปภาพ" แสดง loading spinner
- ✅ ข้อความเปลี่ยนเป็น "กำลังอัพโหลด..."
- ✅ ปุ่มถูก disable (ไม่สามารถคลิกซ้ำได้)
- ✅ ปุ่ม "ยกเลิก" ถูก disable

---

### Test 8: Error Handling ❌

**ขั้นตอน:**
```
1. หยุด backend server
2. ทำตามขั้นตอน 1-10 จาก Test 1
```

**ผลลัพธ์ที่คาดหวัง:**
- ❌ แสดงข้อความ error: "ไม่สามารถอัพโหลดรูปภาพได้"
- ✅ ปุ่มกลับมา enable
- ✅ รูปภาพยังคงเป็นรูปเดิม

---

### Test 9: Authentication Required 🔒

**ขั้นตอน:**
```
1. Logout
2. พยายามเข้าถึง /profile โดยตรง
```

**ผลลัพธ์ที่คาดหวัง:**
- ✅ Redirect ไปหน้า login
- หรือ
- ✅ แสดงข้อความ error: "กรุณา login"

---

### Test 10: Responsive Design 📱

**ขั้นตอน:**
```
1. เปิด DevTools (F12)
2. เปลี่ยนเป็น mobile view (iPhone, Android)
3. ทดสอบอัพโหลดรูป
```

**ผลลัพธ์ที่คาดหวัง:**
- ✅ UI แสดงถูกต้องบน mobile
- ✅ ปุ่มกดได้สะดวก
- ✅ รูปภาพแสดงถูกต้อง

---

## 🔍 การตรวจสอบเพิ่มเติม

### ตรวจสอบไฟล์ที่อัพโหลด

```bash
# ดูไฟล์ใน upload directory
cd wecare-backend/uploads/profiles
ls -la

# ควรเห็นไฟล์รูปภาพ
# profile-1738156800000-123456789.jpg
```

### ตรวจสอบ Database

```bash
# เปิด SQLite
cd wecare-backend/db
sqlite3 wecare.db

# Query user profile
SELECT id, email, full_name, profile_image_url FROM users WHERE email = 'community1@wecare.dev';

# ควรเห็น profile_image_url
# /uploads/profiles/profile-1738156800000-123456789.jpg
```

### ตรวจสอบ Network Request

```
1. เปิด DevTools (F12)
2. ไปที่ tab Network
3. อัพโหลดรูปภาพ
4. ดู request ไปยัง /api/auth/upload-profile-image
```

**ควรเห็น:**
- ✅ Method: POST
- ✅ Status: 200 OK
- ✅ Content-Type: multipart/form-data
- ✅ Response: { message: "...", imageUrl: "..." }

### ตรวจสอบ Console Logs

```
1. เปิด DevTools Console
2. อัพโหลดรูปภาพ
3. ดู logs
```

**ไม่ควรเห็น:**
- ❌ Error messages
- ❌ 404 Not Found
- ❌ 500 Internal Server Error

---

## 📊 Test Results Template

### Test Summary

| Test | Description | Status | Notes |
|------|-------------|--------|-------|
| 1 | อัพโหลดสำเร็จ | ⬜ | |
| 2 | Validation: File Type | ⬜ | |
| 3 | Validation: File Size | ⬜ | |
| 4 | ยกเลิกการอัพโหลด | ⬜ | |
| 5 | แทนที่รูปเดิม | ⬜ | |
| 6 | Hover Effect | ⬜ | |
| 7 | Loading State | ⬜ | |
| 8 | Error Handling | ⬜ | |
| 9 | Authentication | ⬜ | |
| 10 | Responsive Design | ⬜ | |

**Legend:**
- ✅ = Passed
- ❌ = Failed
- ⬜ = Not tested yet
- ⚠️ = Partial pass

---

## 🐛 Common Issues & Solutions

### Issue 1: ไอคอนกล้องไม่แสดง

**สาเหตุ:**
- CSS hover ไม่ทำงาน
- TailwindCSS ไม่ compile

**วิธีแก้:**
- ตรวจสอบ class `group` และ `group-hover:opacity-100`
- รีสตาร์ท frontend

### Issue 2: ไม่สามารถเลือกไฟล์ได้

**สาเหตุ:**
- File input ถูก disabled
- Accept attribute ไม่ถูกต้อง

**วิธีแก้:**
- ตรวจสอบ `accept="image/jpeg,image/jpg,image/png,image/webp"`
- ตรวจสอบว่า input ไม่ถูก disabled

### Issue 3: อัพโหลดแล้วไม่เห็นรูป

**สาเหตุ:**
- API ไม่ return imageUrl
- Static file serving ไม่ทำงาน
- CORS issue

**วิธีแก้:**
- ตรวจสอบ response จาก API
- ตรวจสอบ `app.use('/uploads', express.static(...))`
- ตรวจสอบ CORS configuration

### Issue 4: Error 401 Unauthorized

**สาเหตุ:**
- Token หมดอายุ
- Token ไม่ถูกส่งไปใน header

**วิธีแก้:**
- Login ใหม่
- ตรวจสอบ `Authorization: Bearer {token}` header

### Issue 5: Error 500 Internal Server Error

**สาเหตุ:**
- Backend error
- Upload directory ไม่มี
- Database error

**วิธีแก้:**
- ดู backend console logs
- ตรวจสอบว่า `uploads/profiles/` directory มีอยู่
- ตรวจสอบ database connection

---

## 🎯 Success Criteria

### ✅ ถือว่าสำเร็จเมื่อ:

1. ✅ อัพโหลดรูปภาพได้สำเร็จ
2. ✅ รูปภาพแสดงในโปรไฟล์
3. ✅ Validation ทำงานถูกต้อง
4. ✅ Loading state แสดงถูกต้อง
5. ✅ Error handling ทำงานถูกต้อง
6. ✅ ไม่มี console errors
7. ✅ ไฟล์ถูกบันทึกใน server
8. ✅ Database อัพเดทถูกต้อง
9. ✅ รูปเดิมถูกลบเมื่ออัพโหลดรูปใหม่
10. ✅ Responsive design ทำงานบน mobile

---

## 📝 Test Report Template

```markdown
# Profile Image Upload - Test Report

**Tester:** [ชื่อผู้ทดสอบ]
**Date:** [วันที่]
**Time:** [เวลา]
**Environment:**
- Frontend: http://localhost:5175
- Backend: http://localhost:3001
- Browser: [Chrome/Firefox/Safari]
- OS: [Windows/Mac/Linux]

## Test Results

### Test 1: อัพโหลดสำเร็จ
- Status: [✅/❌]
- Notes: [บันทึก]

### Test 2: Validation - File Type
- Status: [✅/❌]
- Notes: [บันทึก]

[... ต่อไปเรื่อยๆ ...]

## Issues Found

1. [Issue description]
   - Severity: [High/Medium/Low]
   - Steps to reproduce: [...]
   - Expected: [...]
   - Actual: [...]

## Recommendations

1. [แนะนำการปรับปรุง]
2. [...]

## Overall Assessment

- Total Tests: 10
- Passed: [X]
- Failed: [Y]
- Pass Rate: [X/10 * 100]%

**Conclusion:** [สรุปผลการทดสอบ]
```

---

## 🚀 Ready to Test!

**ขั้นตอนถัดไป:**

1. **เปิด Browser**
   ```
   http://localhost:5175
   ```

2. **Login**
   ```
   Email: community1@wecare.dev
   Password: password
   ```

3. **ไปหน้า Profile**
   ```
   คลิกเมนู "โปรไฟล์"
   ```

4. **ทดสอบอัพโหลด**
   ```
   Hover → Click → Select → Upload
   ```

5. **ตรวจสอบผลลัพธ์**
   ```
   ✅ รูปแสดงถูกต้อง
   ✅ ไม่มี errors
   ```

---

**Status:** ✅ **READY TO TEST**

**URL:** http://localhost:5175/profile

**Good Luck! 🎉**
