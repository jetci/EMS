# 📋 Placeholder Guidelines - ป้องกันการป้อนข้อมูลผิดพลาด

**วันที่:** 19 มกราคม 2569 เวลา 22:18  
**วัตถุประสงค์:** กำหนดมาตรฐาน placeholder สำหรับทุก input field  
**เป้าหมาย:** ป้องกันการป้อนข้อมูลผิดพลาด, ปรับปรุง UX

---

## 🎯 หลักการสำคัญ

### 1. ทุก Input ต้องมี Placeholder ✅
```typescript
// ✅ Good - มี placeholder
<input 
    type="text" 
    placeholder="ชื่อ-นามสกุล (เช่น สมชาย ใจดี)" 
/>

// ❌ Bad - ไม่มี placeholder
<input type="text" />
```

### 2. Placeholder ต้องชัดเจนและเป็นตัวอย่างจริง ✅
```typescript
// ✅ Good - ชัดเจน มีตัวอย่าง
<input placeholder="เบอร์โทร (เช่น 0812345678)" />

// ❌ Bad - คลุมเครือ
<input placeholder="กรอกเบอร์โทร" />
```

### 3. ใช้ภาษาไทยที่เข้าใจง่าย ✅
```typescript
// ✅ Good - ภาษาไทยชัดเจน
<input placeholder="ที่อยู่ (เช่น 123 หมู่ 1 ต.ฝาง อ.ฝาง จ.เชียงใหม่)" />

// ❌ Bad - ภาษาอังกฤษหรือคำศัพท์ยาก
<input placeholder="Address" />
```

---

## 📝 Placeholder Standards by Field Type

### 1. ชื่อ-นามสกุล (Name)
```typescript
<input 
    type="text"
    name="fullName"
    placeholder="ชื่อ-นามสกุล (เช่น สมชาย ใจดี)"
    required
/>
```

### 2. เบอร์โทรศัพท์ (Phone)
```typescript
<input 
    type="tel"
    name="phone"
    placeholder="เบอร์โทร 10 หลัก (เช่น 0812345678)"
    pattern="[0-9]{10}"
    required
/>
```

### 3. อีเมล (Email)
```typescript
<input 
    type="email"
    name="email"
    placeholder="อีเมล (เช่น example@wecare.dev)"
    required
/>
```

### 4. เลขบัตรประชาชน (ID Card)
```typescript
<input 
    type="text"
    name="idCard"
    placeholder="เลขบัตรประชาชน 13 หลัก (เช่น 1234567890123)"
    pattern="[0-9]{13}"
    maxLength={13}
    required
/>
```

### 5. ที่อยู่ (Address)
```typescript
<input 
    type="text"
    name="address"
    placeholder="ที่อยู่ (เช่น 123 หมู่ 1 ต.ฝาง อ.ฝาง จ.เชียงใหม่)"
    required
/>
```

### 6. บ้านเลขที่ (House Number)
```typescript
<input 
    type="text"
    name="houseNumber"
    placeholder="บ้านเลขที่ (เช่น 123/45)"
/>
```

### 7. หมู่บ้าน (Village)
```typescript
<input 
    type="text"
    name="village"
    placeholder="หมู่บ้าน (เช่น หมู่ 1 บ้านหนองตุ้ม)"
/>
```

### 8. วันที่ (Date)
```typescript
<ModernDatePicker 
    name="appointmentDate"
    placeholder="เลือกวันนัดหมาย (เช่น 20 ม.ค. 2569)"
    required
/>
```

### 9. เวลา (Time)
```typescript
<ThaiTimePicker 
    name="appointmentTime"
    placeholder="เลือกเวลา (เช่น 14:30)"
    required
/>
```

### 10. ทะเบียนรถ (License Plate)
```typescript
<input 
    type="text"
    name="licensePlate"
    placeholder="ทะเบียนรถ (เช่น กข 1234 เชียงใหม่)"
    required
/>
```

### 11. ค้นหา (Search)
```typescript
<input 
    type="text"
    name="search"
    placeholder="ค้นหาด้วยชื่อ, เบอร์โทร, หรือรหัส..."
/>
```

### 12. รหัสผ่าน (Password)
```typescript
<input 
    type="password"
    name="password"
    placeholder="รหัสผ่าน (อย่างน้อย 8 ตัวอักษร)"
    minLength={8}
    required
/>
```

### 13. จำนวน (Number)
```typescript
<input 
    type="number"
    name="caregiverCount"
    placeholder="จำนวนผู้ดูแล (เช่น 0, 1, 2)"
    min="0"
/>
```

### 14. โรคประจำตัว (Chronic Diseases)
```typescript
<TagInput 
    tags={chronicDiseases}
    setTags={setChronicDiseases}
    placeholder="พิมพ์โรคประจำตัว (เช่น เบาหวาน, ความดันโลหิตสูง)"
/>
```

### 15. อาการแพ้ (Allergies)
```typescript
<TagInput 
    tags={allergies}
    setTags={setAllergies}
    placeholder="พิมพ์สิ่งที่แพ้ (เช่น ยาปฏิชีวนะ, อาหารทะเล)"
/>
```

---

## 🎨 Placeholder Examples by Page

### 1. Patient Registration (ลงทะเบียนผู้ป่วย)
```typescript
// ข้อมูลส่วนตัว
<input placeholder="ชื่อ-นามสกุล (เช่น สมชาย ใจดี)" />
<input placeholder="เลขบัตรประชาชน 13 หลัก (เช่น 1234567890123)" />
<input placeholder="เบอร์โทร 10 หลัก (เช่น 0812345678)" />
<ModernDatePicker placeholder="วันเกิด (เช่น 15 ม.ค. 2510)" />

// ที่อยู่
<input placeholder="บ้านเลขที่ (เช่น 123/45)" />
<input placeholder="หมู่บ้าน (เช่น หมู่ 1 บ้านหนองตุ้ม)" />
<input placeholder="ตำบล (เช่น ฝาง)" />
<input placeholder="อำเภอ (เช่น ฝาง)" />
<input placeholder="จังหวัด (เช่น เชียงใหม่)" />

// ข้อมูลสุขภาพ
<TagInput placeholder="โรคประจำตัว (เช่น เบาหวาน, ความดันโลหิตสูง)" />
<TagInput placeholder="อาการแพ้ (เช่น ยาปฏิชีวนะ, อาหารทะเล)" />
<input placeholder="หมายเหตุเพิ่มเติม (ถ้ามี)" />
```

### 2. Ride Request (ขอใช้บริการ)
```typescript
<input placeholder="จุดรับผู้ป่วย (เช่น บ้านเลขที่ 123 หมู่ 1 ต.ฝาง)" />
<input placeholder="จุดหมายปลายทาง (เช่น โรงพยาบาลฝาง)" />
<ModernDatePicker placeholder="วันนัดหมาย (เช่น 20 ม.ค. 2569)" />
<ThaiTimePicker placeholder="เวลานัดหมาย (เช่น 14:30)" />
<input placeholder="เบอร์โทรติดต่อ (เช่น 0812345678)" />
<input placeholder="จำนวนผู้ดูแล (เช่น 0, 1, 2)" />
<TagInput placeholder="ความต้องการพิเศษ (เช่น ต้องใช้วีลแชร์)" />
```

### 3. Driver Registration (ลงทะเบียนคนขับ)
```typescript
<input placeholder="ชื่อ-นามสกุล (เช่น สมชาย ใจดี)" />
<input placeholder="เบอร์โทร 10 หลัก (เช่น 0812345678)" />
<input placeholder="เลขใบขับขี่ (เช่น 12345678)" />
<ModernDatePicker placeholder="วันหมดอายุใบขับขี่ (เช่น 31 ธ.ค. 2570)" />
```

### 4. Vehicle Registration (ลงทะเบียนรถ)
```typescript
<input placeholder="ทะเบียนรถ (เช่น กข 1234 เชียงใหม่)" />
<input placeholder="ยี่ห้อ (เช่น Toyota)" />
<input placeholder="รุ่น (เช่น Commuter)" />
<input placeholder="ปีที่ผลิต (เช่น 2020)" />
<input placeholder="สี (เช่น ขาว)" />
```

### 5. Search Fields (ช่องค้นหา)
```typescript
// ค้นหาผู้ป่วย
<input placeholder="ค้นหาผู้ป่วย (ชื่อ, เลขบัตร, เบอร์โทร)" />

// ค้นหาการเดินทาง
<input placeholder="ค้นหาการเดินทาง (ชื่อผู้ป่วย, Ride ID)" />

// ค้นหาคนขับ
<input placeholder="ค้นหาคนขับ (ชื่อ, เบอร์โทร)" />

// ค้นหารถ
<input placeholder="ค้นหารถ (ทะเบียน, ยี่ห้อ)" />
```

---

## 🎓 Best Practices

### 1. เพิ่มรูปแบบที่ต้องการ (Format) ✅
```typescript
// ✅ Good - บอกรูปแบบชัดเจน
<input placeholder="เบอร์โทร 10 หลัก (เช่น 0812345678)" />

// ❌ Bad - ไม่บอกรูปแบบ
<input placeholder="เบอร์โทร" />
```

### 2. ให้ตัวอย่างที่เป็นจริง ✅
```typescript
// ✅ Good - ตัวอย่างจริง
<input placeholder="ที่อยู่ (เช่น 123 หมู่ 1 ต.ฝาง อ.ฝาง จ.เชียงใหม่)" />

// ❌ Bad - ตัวอย่างไม่ชัดเจน
<input placeholder="ที่อยู่ (เช่น xxx)" />
```

### 3. ระบุข้อจำกัด (Constraints) ✅
```typescript
// ✅ Good - บอกข้อจำกัด
<input placeholder="รหัสผ่าน (อย่างน้อย 8 ตัวอักษร)" minLength={8} />

// ❌ Bad - ไม่บอกข้อจำกัด
<input placeholder="รหัสผ่าน" />
```

### 4. ใช้ภาษาที่เข้าใจง่าย ✅
```typescript
// ✅ Good - ภาษาง่าย
<input placeholder="เบอร์โทร 10 หลัก" />

// ❌ Bad - ภาษายาก
<input placeholder="หมายเลขโทรศัพท์มือถือ" />
```

### 5. เพิ่ม Hint สำหรับ Optional Fields ✅
```typescript
// ✅ Good - บอกว่า optional
<input placeholder="หมายเหตุเพิ่มเติม (ถ้ามี)" />

// ❌ Bad - ไม่บอก
<input placeholder="หมายเหตุ" />
```

---

## 🔍 Validation + Placeholder

### Pattern ที่ดี:
```typescript
<div>
    <label>เบอร์โทรศัพท์ *</label>
    <input 
        type="tel"
        name="phone"
        placeholder="เบอร์โทร 10 หลัก (เช่น 0812345678)"
        pattern="[0-9]{10}"
        maxLength={10}
        required
    />
    <small className="text-gray-500">
        กรุณากรอกเบอร์โทร 10 หลัก เริ่มต้นด้วย 0
    </small>
</div>
```

### ประโยชน์:
1. ✅ **Label** - บอกว่าต้องกรอกอะไร
2. ✅ **Placeholder** - แสดงตัวอย่าง
3. ✅ **Pattern** - Validate รูปแบบ
4. ✅ **Helper Text** - อธิบายเพิ่มเติม

---

## 📊 Implementation Checklist

### ตรวจสอบทุกหน้า:
- [ ] Patient Registration
- [ ] Ride Request
- [ ] Driver Registration
- [ ] Vehicle Registration
- [ ] User Management
- [ ] Search Forms
- [ ] Filter Forms
- [ ] Settings Pages

### ตรวจสอบทุก Input Type:
- [ ] Text inputs
- [ ] Email inputs
- [ ] Phone inputs
- [ ] Number inputs
- [ ] Date pickers
- [ ] Time pickers
- [ ] Select dropdowns
- [ ] TextAreas
- [ ] Tag inputs

---

## 🎯 Quick Reference

### Template สำหรับ Input ทั่วไป:
```typescript
<input 
    type="text"
    name="fieldName"
    placeholder="คำอธิบาย (เช่น ตัวอย่างที่ชัดเจน)"
    required={true/false}
    pattern="regex_if_needed"
    maxLength={number}
    minLength={number}
/>
```

### Template สำหรับ Date:
```typescript
<ModernDatePicker 
    name="fieldName"
    placeholder="เลือกวันที่ (เช่น 20 ม.ค. 2569)"
    min={minDate}
    max={maxDate}
    required
/>
```

### Template สำหรับ Search:
```typescript
<input 
    type="text"
    placeholder="ค้นหาด้วย [field1], [field2], หรือ [field3]..."
    value={searchTerm}
    onChange={handleSearch}
/>
```

---

## 💡 Tips

### 1. ใช้ตัวอย่างจากข้อมูลจริง
- ✅ "เช่น 0812345678" (เบอร์จริง)
- ❌ "เช่น 08xxxxxxxx" (ไม่ชัดเจน)

### 2. ระบุหน่วยถ้าจำเป็น
- ✅ "น้ำหนัก (กิโลกรัม)"
- ✅ "ระยะทาง (กิโลเมตร)"

### 3. ใช้คำที่คุ้นเคย
- ✅ "เบอร์โทร" (คุ้นเคย)
- ❌ "หมายเลขโทรศัพท์" (เป็นทางการเกินไป)

### 4. เพิ่ม Context
- ✅ "เบอร์โทรผู้ป่วย หรือผู้ดูแล"
- ❌ "เบอร์โทร"

---

## 📄 Summary

### ทุก Input ควรมี:
1. ✅ **Label** - ชื่อ field
2. ✅ **Placeholder** - ตัวอย่างข้อมูล
3. ✅ **Validation** - ตรวจสอบความถูกต้อง
4. ✅ **Helper Text** - คำอธิบายเพิ่มเติม (ถ้าจำเป็น)

### ผลลัพธ์:
- ✅ ลดข้อผิดพลาดในการป้อนข้อมูล
- ✅ UX ดีขึ้น
- ✅ ผู้ใช้เข้าใจง่ายขึ้น
- ✅ ลดเวลาในการกรอกฟอร์ม

---

**Created by:** Antigravity AI Assistant  
**Date:** 19 มกราคม 2569  
**Time:** 22:18  
**Status:** ✅ GUIDELINES COMPLETE
