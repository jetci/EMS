# แก้ไข: ปัญหาการอัพโหลดรูปภาพในหน้าโปรไฟล์และการตั้งค่า (Community Profile)

## วันที่: 2026-01-28 23:24

---

## ปัญหาที่พบ

**หน้า:** โปรไฟล์และการตั้งค่า (CommunityProfilePage)

**ขั้นตอนที่ทำให้เกิดปัญหา:**
1. คลิกปุ่ม "แก้ไขข้อมูล"
2. คลิกปุ่ม "เปลี่ยนรูปภาพ"
3. เลือก "เลือกรูปภาพจากคลัง"
4. กด Cancel (ไม่ได้เลือกไฟล์)

**ผลลัพธ์ที่ผิดพลาด:**
- ✅ ระบบแสดงข้อความ "อัปโหลดรูปภาพสำเร็จแล้ว" ทันที
- ❌ แม้ว่าผู้ใช้จะไม่ได้เลือกไฟล์จริง

---

## สาเหตุ

### ไฟล์: `src/pages/CommunityProfilePage.tsx`

**บรรทัด 145-149 (โค้ดเดิม):**
```typescript
{
    label: "เลือกรูปภาพจากคลัง",
    icon: ImageIcon,
    onClick: () => {
        console.log("Choose from library");
        setIsActionSheetOpen(false);
        showToast("อัปโหลดรูปภาพสำเร็จแล้ว"); // ❌ แสดงทันทีโดยไม่ตรวจสอบ
    }
}
```

**ปัญหา:**
1. ไม่มี file input element สำหรับเลือกไฟล์
2. แสดง toast ทันทีโดยไม่รอให้ผู้ใช้เลือกไฟล์
3. ไม่มีการ validate ประเภทและขนาดไฟล์
4. ไม่มีการจัดการ error

---

## การแก้ไข

### 1. เพิ่มฟังก์ชัน `handleImageSelect`

```typescript
const handleImageSelect = (e: React.ChangeEvent<HTMLInputElement>) => {
    const files = e.target.files;
    
    // 1. ตรวจสอบว่าผู้ใช้เลือกไฟล์จริง
    if (!files || files.length === 0) {
        // ผู้ใช้กด Cancel หรือไม่ได้เลือกไฟล์
        return;
    }
    
    const file = files[0];
    
    // 2. ตรวจสอบประเภทไฟล์
    const validTypes = ['image/png', 'image/jpeg', 'image/jpg', 'image/gif'];
    if (!validTypes.includes(file.type)) {
        showToast('❌ กรุณาเลือกไฟล์รูปภาพ (PNG, JPG, GIF) เท่านั้น');
        e.target.value = ''; // Reset input
        return;
    }
    
    // 3. ตรวจสอบขนาดไฟล์ (ไม่เกิน 1MB)
    const maxSize = 1 * 1024 * 1024; // 1MB
    if (file.size > maxSize) {
        showToast('❌ ขนาดไฟล์ต้องไม่เกิน 1MB');
        e.target.value = ''; // Reset input
        return;
    }
    
    // 4. อ่านและแสดงตัวอย่างรูปภาพ
    const reader = new FileReader();
    reader.onloadend = () => {
        setProfileImage(reader.result as string);
        showToast('✅ อัปโหลดรูปภาพสำเร็จแล้ว!');
    };
    reader.onerror = () => {
        showToast('❌ เกิดข้อผิดพลาดในการอ่านไฟล์');
        e.target.value = ''; // Reset input
    };
    reader.readAsDataURL(file);
};
```

### 2. แก้ไข ActionSheet Option

```typescript
{
    label: "เลือกรูปภาพจากคลัง",
    icon: ImageIcon,
    onClick: () => {
        setIsActionSheetOpen(false);
        // Trigger file input click
        const fileInput = document.getElementById('profile-image-input') as HTMLInputElement;
        if (fileInput) {
            fileInput.click();
        }
    }
}
```

### 3. เพิ่ม Hidden File Input Element

```tsx
{isEditing && (
    <div>
        <button type="button" onClick={() => setIsActionSheetOpen(true)}>
            เปลี่ยนรูปภาพ
        </button>
        <p className="text-xs text-gray-500 mt-1">JPG, GIF or PNG. 1MB max.</p>
        {/* Hidden file input */}
        <input
            id="profile-image-input"
            type="file"
            accept="image/png, image/jpeg, image/jpg, image/gif"
            onChange={handleImageSelect}
            className="hidden"
            aria-label="เลือกรูปภาพโปรไฟล์"
        />
    </div>
)}
```

### 4. แก้ไข API Parameter

```typescript
// เปลี่ยนจาก fullName เป็น name
await authAPI.updateProfile({
    name: `${formData.firstName} ${formData.lastName}`.trim(),
    phone: formData.phone,
});
```

---

## ผลลัพธ์หลังแก้ไข

### Test Cases

| กรณี | พฤติกรรมเดิม | พฤติกรรมใหม่ | สถานะ |
|------|-------------|-------------|-------|
| กด Cancel | แสดง "สำเร็จ" ทันที | ไม่มีการเปลี่ยนแปลง | ✅ แก้แล้ว |
| เลือกไฟล์ .txt | ไม่มีการตรวจสอบ | แสดง error "กรุณาเลือกรูปภาพ" | ✅ แก้แล้ว |
| เลือกไฟล์ > 1MB | ไม่มีการตรวจสอบ | แสดง error "ขนาดไฟล์เกิน 1MB" | ✅ แก้แล้ว |
| เลือกรูปภาพถูกต้อง | - | แสดงตัวอย่าง + "สำเร็จ" | ✅ ทำงานถูกต้อง |
| เกิด error อ่านไฟล์ | ไม่มีการจัดการ | แสดง error "เกิดข้อผิดพลาด" | ✅ แก้แล้ว |

---

## การทดสอบ

### ขั้นตอนการทดสอบ:

1. **เข้าสู่ระบบ**
   - URL: http://localhost:5173
   - User: community user account

2. **ไปที่หน้าโปรไฟล์**
   - คลิกเมนู "โปรไฟล์และการตั้งค่า"

3. **ทดสอบการแก้ไขรูปภาพ**
   - คลิก "แก้ไขข้อมูล"
   - คลิก "เปลี่ยนรูปภาพ"
   - เลือก "เลือกรูปภาพจากคลัง"

4. **ทดสอบแต่ละกรณี:**

   **Test 1: กด Cancel**
   - คลิกเลือกรูปภาพ
   - กด Cancel
   - ✅ ไม่มี toast แสดง
   - ✅ รูปภาพไม่เปลี่ยน

   **Test 2: เลือกไฟล์ผิดประเภท**
   - คลิกเลือกรูปภาพ
   - เลือกไฟล์ .txt หรือ .pdf
   - ✅ แสดง toast "❌ กรุณาเลือกไฟล์รูปภาพ (PNG, JPG, GIF) เท่านั้น"

   **Test 3: เลือกไฟล์ขนาดใหญ่**
   - คลิกเลือกรูปภาพ
   - เลือกรูปภาพขนาด > 1MB
   - ✅ แสดง toast "❌ ขนาดไฟล์ต้องไม่เกิน 1MB"

   **Test 4: เลือกรูปภาพถูกต้อง**
   - คลิกเลือกรูปภาพ
   - เลือกรูปภาพ PNG/JPG < 1MB
   - ✅ แสดงตัวอย่างรูปภาพทันที
   - ✅ แสดง toast "✅ อัปโหลดรูปภาพสำเร็จแล้ว!"

---

## ไฟล์ที่แก้ไข

1. **src/pages/CommunityProfilePage.tsx**
   - เพิ่มฟังก์ชัน `handleImageSelect` (บรรทัด 141-179)
   - แก้ไข `actionSheetOptions` (บรรทัด 181-202)
   - เพิ่ม hidden file input (บรรทัด 233-240)
   - แก้ไข API parameter จาก `fullName` เป็น `name` (บรรทัด 79)

---

## การปรับปรุงเพิ่มเติม

### ความปลอดภัย
✅ ตรวจสอบประเภทไฟล์ (PNG, JPG, GIF เท่านั้น)
✅ จำกัดขนาดไฟล์ (ไม่เกิน 1MB)
✅ Reset input เมื่อเกิด error

### UX/UI
✅ แสดง toast เฉพาะเมื่อมีการเปลี่ยนแปลงจริง
✅ แสดงข้อความ error ที่ชัดเจน
✅ แสดงตัวอย่างรูปภาพทันที

### Accessibility
✅ เพิ่ม `aria-label` สำหรับ hidden input
✅ รองรับ keyboard navigation

### Error Handling
✅ จัดการ error การอ่านไฟล์
✅ Reset input เมื่อเกิด error
✅ แสดงข้อความ error ที่เข้าใจง่าย

---

## สถานะ

✅ **แก้ไขเสร็จสมบูรณ์**
📝 **รอการทดสอบจาก SA**

---

## หมายเหตุ

- การแก้ไขนี้ใช้ได้กับหน้า **CommunityProfilePage** เท่านั้น
- หน้า **AdminSystemSettingsPage** ได้รับการแก้ไขแยกต่างหากแล้ว
- ทั้งสองหน้ามีปัญหาคล้ายกัน แต่ใช้ component และ logic ต่างกัน

---

## เอกสารที่เกี่ยวข้อง

- `test-logo-upload-fix.md` - การแก้ไขสำหรับ AdminSystemSettingsPage
- `test-logo-upload.ps1` - Script ทดสอบ
