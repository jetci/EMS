# การแก้ไข: ปัญหาการอัพโหลดโลโก้ในหน้าการตั้งค่า

## ปัญหาที่พบ
เมื่อผู้ใช้:
1. คลิกปุ่ม "อัปโหลดโลโก้"
2. กด Cancel (ไม่ได้เลือกไฟล์)
3. กดปุ่ม "บันทึก"

**ผลลัพธ์**: ระบบแสดงข้อความ "บันทึกการตั้งค่าสำเร็จ!" แม้ว่าจะไม่มีการเปลี่ยนแปลงอะไร

## สาเหตุ
ฟังก์ชัน `handleLogoChange` ในไฟล์ `AdminSystemSettingsPage.tsx` ไม่มีการ:
- ตรวจสอบว่าผู้ใช้เลือกไฟล์จริงหรือไม่
- Validate ประเภทไฟล์
- Validate ขนาดไฟล์
- จัดการ error ที่อาจเกิดขึ้น

## การแก้ไข

### ไฟล์: `src/pages/AdminSystemSettingsPage.tsx`

เพิ่มการตรวจสอบและ validation:

```typescript
const handleLogoChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    const files = e.target.files;
    
    // 1. ตรวจสอบว่าผู้ใช้เลือกไฟล์จริง
    if (!files || files.length === 0) {
        // ผู้ใช้กด Cancel หรือไม่ได้เลือกไฟล์
        return;
    }
    
    const file = files[0];
    
    // 2. ตรวจสอบประเภทไฟล์
    const validTypes = ['image/png', 'image/jpeg', 'image/jpg', 'image/svg+xml'];
    if (!validTypes.includes(file.type)) {
        alert('กรุณาเลือกไฟล์รูปภาพ (PNG, JPG, หรือ SVG) เท่านั้น');
        e.target.value = ''; // Reset input
        return;
    }
    
    // 3. ตรวจสอบขนาดไฟล์ (ไม่เกิน 2MB)
    const maxSize = 2 * 1024 * 1024; // 2MB
    if (file.size > maxSize) {
        alert('ขนาดไฟล์ต้องไม่เกิน 2MB');
        e.target.value = ''; // Reset input
        return;
    }
    
    // 4. อ่านและแสดงตัวอย่างรูปภาพ
    const reader = new FileReader();
    reader.onloadend = () => {
        setSettings(prev => ({ ...prev, logoUrl: reader.result as string }));
    };
    reader.onerror = () => {
        alert('เกิดข้อผิดพลาดในการอ่านไฟล์');
        e.target.value = ''; // Reset input
    };
    reader.readAsDataURL(file);
};
```

## การทำงานหลังแก้ไข

### กรณีที่ 1: ผู้ใช้กด Cancel
- ✅ ไม่มีการเปลี่ยนแปลงใดๆ
- ✅ ไม่มี alert แสดง
- ✅ ต้องกดปุ่ม "บันทึก" ถึงจะแสดง alert "บันทึกการตั้งค่าสำเร็จ!"

### กรณีที่ 2: เลือกไฟล์ที่ไม่ใช่รูปภาพ
- ❌ แสดง alert "กรุณาเลือกไฟล์รูปภาพ (PNG, JPG, หรือ SVG) เท่านั้น"
- ✅ Reset input field
- ✅ ไม่มีการเปลี่ยนแปลงโลโก้

### กรณีที่ 3: เลือกไฟล์ขนาดใหญ่เกิน 2MB
- ❌ แสดง alert "ขนาดไฟล์ต้องไม่เกิน 2MB"
- ✅ Reset input field
- ✅ ไม่มีการเปลี่ยนแปลงโลโก้

### กรณีที่ 4: เลือกไฟล์รูปภาพที่ถูกต้อง
- ✅ แสดงตัวอย่างรูปภาพทันที
- ✅ เมื่อกด "บันทึก" จะบันทึกโลโก้ใหม่
- ✅ แสดง alert "บันทึกการตั้งค่าสำเร็จ!"

### กรณีที่ 5: เกิด error ในการอ่านไฟล์
- ❌ แสดง alert "เกิดข้อผิดพลาดในการอ่านไฟล์"
- ✅ Reset input field
- ✅ ไม่มีการเปลี่ยนแปลงโลโก้

## ประโยชน์ที่ได้รับ

1. **UX ที่ดีขึ้น**: ผู้ใช้ไม่เห็นข้อความสำเร็จเมื่อไม่มีการเปลี่ยนแปลง
2. **ความปลอดภัย**: ตรวจสอบประเภทและขนาดไฟล์
3. **Error Handling**: จัดการ error ที่อาจเกิดขึ้นได้ดีขึ้น
4. **Feedback ที่ชัดเจน**: แจ้งเตือนเมื่อมีปัญหา

## การทดสอบ

### ขั้นตอนการทดสอบ:
1. เข้าหน้า "ตั้งค่าระบบ" (Admin System Settings)
2. คลิกปุ่ม "อัปโหลดโลโก้"
3. ทดสอบแต่ละกรณี:
   - กด Cancel → ไม่มี alert
   - เลือกไฟล์ .txt → แสดง error
   - เลือกรูปขนาด 5MB → แสดง error
   - เลือกรูป .png ขนาด 500KB → แสดงตัวอย่าง
4. กดปุ่ม "บันทึก" → แสดง "บันทึกการตั้งค่าสำเร็จ!"

## สถานะ
✅ แก้ไขเสร็จสมบูรณ์
📝 รอการทดสอบจาก SA
