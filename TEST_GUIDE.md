# 🧪 EMS WeCare - คู่มือการทดสอบแอปพลิเคชัน

## 📋 สคริปต์ทดสอบที่มีให้ใช้งาน

### 1. **test-app-comprehensive.ps1** - การทดสอบแบบครอบคลุม
ทดสอบฟังก์ชันหลักทั้งหมดของระบบ EMS

**ฟีเจอร์ที่ทดสอบ:**
- ✅ Backend Connection
- ✅ Authentication (ทุก Role)
- ✅ Patient Management (CRUD)
- ✅ Ride Management
- ✅ Driver Management
- ✅ Vehicle Management
- ✅ Reports & Analytics
- ✅ Audit Logs
- ✅ Security & Authorization
- ✅ Data Isolation

**วิธีใช้งาน:**
```powershell
# รันแบบพื้นฐาน
.\test-app-comprehensive.ps1

# รันพร้อมแสดงรายละเอียด
.\test-app-comprehensive.ps1 -Verbose

# หยุดทันทีเมื่อเจอ Error
.\test-app-comprehensive.ps1 -StopOnError

# กำหนด Base URL
.\test-app-comprehensive.ps1 -BaseUrl "http://localhost:5000/api"
```

**ผลลัพธ์:**
- แสดงผลการทดสอบแบบ Real-time
- สรุปจำนวน Pass/Fail
- บันทึกผลลงไฟล์ JSON: `test-results-[timestamp].json`

---

### 2. **test-e2e-full-workflow.ps1** - การทดสอบ End-to-End
จำลองการใช้งานจริงของผู้ใช้ทุก Role ตั้งแต่ต้นจนจบ

**Scenarios ที่ทดสอบ:**
1. **Community User Journey**
   - Login
   - ลงทะเบียนผู้ป่วย
   - ดูข้อมูลผู้ป่วย
   - ขอรถพยาบาล

2. **Office User Journey**
   - Login
   - ดูรายการขอรถที่รอดำเนินการ
   - หาคนขับที่ว่าง
   - มอบหมายคนขับ
   - ดูข้อมูลรถพยาบาล

3. **Driver User Journey**
   - Login
   - ดูรายการที่ได้รับมอบหมาย
   - เริ่มการเดินทาง
   - อัพเดทตำแหน่ง
   - ปิดงาน

4. **Executive User Journey**
   - Login
   - ดู Dashboard Analytics
   - สร้างรายงาน
   - ดู Performance Metrics

5. **Admin User Journey**
   - Login
   - ดูรายการผู้ใช้ทั้งหมด
   - ดู Audit Logs
   - ดูการตั้งค่าระบบ

**วิธีใช้งาน:**
```powershell
# รันแบบพื้นฐาน
.\test-e2e-full-workflow.ps1

# กำหนด Base URL
.\test-e2e-full-workflow.ps1 -BaseUrl "http://localhost:5000/api"
```

**ผลลัพธ์:**
- แสดงขั้นตอนการทำงานแบบละเอียด
- จำลองการใช้งานจริงของผู้ใช้
- ทำความสะอาดข้อมูลทดสอบอัตโนมัติ

---

### 3. **test-performance.ps1** - การทดสอบประสิทธิภาพ
ทดสอบความเร็วและความสามารถรับโหลดของระบบ

**การทดสอบที่รวมอยู่:**
1. **Single User Baseline** - วัดประสิทธิภาพพื้นฐาน
2. **Concurrent User Load Test** - ทดสอบผู้ใช้พร้อมกัน
3. **Response Time Analysis** - วิเคราะห์เวลาตอบสนอง (P50, P95, P99)
4. **Stress Test** - ทดสอบ CRUD Operations
5. **Database Query Performance** - ทดสอบความเร็ว Query

**วิธีใช้งาน:**
```powershell
# รันแบบพื้นฐาน (10 users, 5 requests each)
.\test-performance.ps1

# กำหนดจำนวนผู้ใช้พร้อมกัน
.\test-performance.ps1 -ConcurrentUsers 20

# กำหนดจำนวน Request ต่อผู้ใช้
.\test-performance.ps1 -RequestsPerUser 10

# แสดงรายละเอียดทุก Request
.\test-performance.ps1 -DetailedOutput

# รันแบบเต็มรูปแบบ
.\test-performance.ps1 -ConcurrentUsers 50 -RequestsPerUser 10 -DetailedOutput
```

**ผลลัพธ์:**
- Response Time Statistics (Average, Min, Max, P50, P95, P99)
- Performance Rating (⭐⭐⭐⭐⭐)
- Requests per Second
- Success Rate
- คำแนะนำในการปรับปรุง
- บันทึกผลลงไฟล์ JSON: `performance-test-[timestamp].json`

---

## 🚀 วิธีเริ่มต้นใช้งาน

### ขั้นตอนที่ 1: เตรียมระบบ
```powershell
# เริ่ม Backend Server
cd wecare-backend
npm run dev

# เริ่ม Frontend (Terminal ใหม่)
npm run dev
```

### ขั้นตอนที่ 2: รันการทดสอบ

**สำหรับการทดสอบครั้งแรก (แนะนำ):**
```powershell
# 1. ทดสอบแบบครอบคลุมก่อน
.\test-app-comprehensive.ps1 -Verbose

# 2. ถ้าผ่าน ลองทดสอบ E2E
.\test-e2e-full-workflow.ps1

# 3. สุดท้าย ทดสอบประสิทธิภาพ
.\test-performance.ps1
```

**สำหรับการทดสอบประจำวัน:**
```powershell
# รันทดสอบแบบเร็ว
.\test-app-comprehensive.ps1
```

**สำหรับการทดสอบก่อน Deploy:**
```powershell
# รันทดสอบทั้งหมด
.\test-app-comprehensive.ps1 -Verbose
.\test-e2e-full-workflow.ps1
.\test-performance.ps1 -ConcurrentUsers 20
```

---

## 📊 การอ่านผลการทดสอบ

### สัญลักษณ์
- ✓ **PASS** (สีเขียว) = ทดสอบผ่าน
- ✗ **FAIL** (สีแดง) = ทดสอบไม่ผ่าน
- ⚠ **WARNING** (สีเหลือง) = มีข้อควรระวัง

### Performance Rating
- ⭐⭐⭐⭐⭐ **Excellent** - Average < 100ms
- ⭐⭐⭐⭐ **Good** - Average < 300ms
- ⭐⭐⭐ **Fair** - Average < 500ms
- ⭐⭐ **Poor** - Average < 1000ms
- ⭐ **Very Poor** - Average > 1000ms

---

## 🔧 การแก้ไขปัญหา

### ปัญหา: Backend ไม่ตอบสนอง
```powershell
# ตรวจสอบว่า Backend ทำงานอยู่หรือไม่
curl http://localhost:5000/api/health

# Restart Backend
cd wecare-backend
npm run dev
```

### ปัญหา: Login ไม่ผ่าน
```powershell
# ตรวจสอบ User Credentials
.\check-all-logins.ps1

# Reset Database (ระวัง: จะลบข้อมูลทั้งหมด)
.\reset-db.ps1
```

### ปัญหา: Test ล้มเหลวบางส่วน
```powershell
# รันแบบ Verbose เพื่อดูรายละเอียด
.\test-app-comprehensive.ps1 -Verbose

# รันแบบหยุดเมื่อเจอ Error
.\test-app-comprehensive.ps1 -StopOnError
```

---

## 📝 สคริปต์ทดสอบอื่นๆ ที่มีอยู่

### ทดสอบเฉพาะ Role
- `test-admin-workflow.ps1` - ทดสอบ Admin
- `test-community-complete.ps1` - ทดสอบ Community
- `test-driver-flow.ps1` - ทดสอบ Driver
- `test-office-dashboard.ps1` - ทดสอบ Office
- `test-executive-dashboard.ps1` - ทดสอบ Executive

### ทดสอบเฉพาะฟีเจอร์
- `test-ride-conflict.ps1` - ทดสอบ Ride Conflict Detection
- `test-driver-locations.ps1` - ทดสอบ Driver Location Tracking
- `test-map-api.ps1` - ทดสอบ Map API
- `test-reports.ps1` - ทดสอบ Reports

### ทดสอบความปลอดภัย
- `test-community-security.ps1` - ทดสอบ Community Security
- `test-driver-security.ps1` - ทดสอบ Driver Security
- `test-radio-security.ps1` - ทดสอบ Radio Security

---

## 🎯 Best Practices

### 1. รันทดสอบก่อน Commit
```powershell
# ก่อน git commit
.\test-app-comprehensive.ps1
```

### 2. รันทดสอบหลังแก้ไข Bug
```powershell
# หลังแก้ไข
.\test-app-comprehensive.ps1 -Verbose
.\test-e2e-full-workflow.ps1
```

### 3. รันทดสอบประสิทธิภาพเป็นระยะ
```powershell
# ทุกสัปดาห์
.\test-performance.ps1 -ConcurrentUsers 20 -RequestsPerUser 10
```

### 4. เก็บ Test Results
```powershell
# Test results จะถูกบันทึกอัตโนมัติที่:
# - test-results-[timestamp].json
# - performance-test-[timestamp].json
```

---

## 📈 การวิเคราะห์ผลการทดสอบ

### ดู Test Results
```powershell
# ดูผลการทดสอบล่าสุด
Get-Content (Get-ChildItem test-results-*.json | Sort-Object LastWriteTime -Descending | Select-Object -First 1).FullName | ConvertFrom-Json
```

### เปรียบเทียบประสิทธิภาพ
```powershell
# ดูผล Performance Tests ทั้งหมด
Get-ChildItem performance-test-*.json | ForEach-Object {
    $data = Get-Content $_.FullName | ConvertFrom-Json
    Write-Host "$($_.Name): Avg Response Time = $($data.Results.AverageResponseTime)ms"
}
```

---

## 🆘 ติดต่อและรายงานปัญหา

หากพบปัญหาในการทดสอบ:
1. เก็บ Test Results JSON file
2. บันทึก Error Messages
3. ตรวจสอบ Backend Logs
4. รายงานใน Issue Tracker

---

## 📚 เอกสารเพิ่มเติม

- [README.md](./README.md) - คู่มือหลักของโปรเจกต์
- [README_TESTS.md](./README_TESTS.md) - รายละเอียดการทดสอบเพิ่มเติม
- [api_requirements.md](./api_requirements.md) - API Documentation

---

**สร้างเมื่อ:** 2026-01-01  
**เวอร์ชัน:** 1.0.0  
**ผู้พัฒนา:** EMS WeCare Team
